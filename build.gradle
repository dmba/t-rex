apply from: 'versions.gradle'

buildscript {
    apply from: 'versions.gradle'

    repositories {
        jcenter()
        google()
    }
}

allprojects {
    repositories {
        jcenter()
        google()
    }
}
